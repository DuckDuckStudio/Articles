name: 检查本地路径

on:
  push:
    branches:
      - main
  pull_request:

permissions: {}

jobs:
  check-local-paths:
    runs-on: ubuntu-latest

    steps:
      - name: 检查本地路径
        uses: DuckDuckStudio/Check_Locale_Paths@21b6719797a806d8f3e60bdd1a945c133f65dcaa # 2.0.5
